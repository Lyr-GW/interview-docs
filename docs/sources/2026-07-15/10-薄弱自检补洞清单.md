# 10 · 薄弱自检补洞清单（明早上场前）

> **本夜续批**（2026-07-15）  
> 对照 [`05` 文末薄弱自检](./05-综合快问快答库-扩编.md) + [`00` 验收](./00-通宵优化计划与进度.md)。  
> 标记：✅已补（有可背卡）/ ⚠仍薄（机制有、实测或口述仍欠）/ 行动项。

## 交叉引用

| 文档 | 用途 |
|------|------|
| [`00`](./00-通宵优化计划与进度.md) | 风险摘要与 8:00 验收勾选 |
| [`05`](./05-综合快问快答库-扩编.md) | ~108 题库 + 原自检表 |
| 本夜卡 | `01`–`09`（口述 / 红线 / 边界 / 算子 / PD / 软题 / vLLM 配置） |

---

## 1 · 逐项对照（05 自检 × 本夜产出）

| 自检项 | 状态 | 落点 | 行动项（未 ✅ 必做） |
|--------|------|------|---------------------|
| 编译缓存脱口 **FIFO/100** | ✅已补 | `03`、`01` 红线 | 被甩 LRU/128 立刻订正一句 |
| TTFT−70% = **代表性测算** | ✅已补 | `02`、`03` | 白板 1187→351；禁「客户 raw log」 |
| Dynamo 60s + cost 白板 | ✅已补 | `01` A | 默写 `adjusted_prefill` 不卡 |
| 投机拒绝采样 + EAGLE→MTP→DSpark | ✅已补 | `01` B | 「无损」= 分布一致，非吞吐保证 |
| 量化决策树 + FP8 四要素 | ✅已补 | `01` C | 不反说 W4 / FP8 场景 |
| Profiling 四层 + 吞吐决策树 | ✅已补 | `01` D | 先 metrics 再 nsys；GPU 空/满分叉 |
| guidance「预留」边界 | ✅已补 | `02` | 线上仅 xgrammar；勿说有 guidance |
| MTP×SO | ✅已补 | `02` | InferParam **硬互斥**；勿编分层已打通 |
| Mooncake 分层一分钟 | ⚠仍薄 | 深文 04/10/11；本夜 `04`/`07` 边界 | **行动**：背 Conductor/Store/TE 三句；勿报自制加速比 |
| Spec V2 / LMCache | ⚠仍薄 | `04` 机制开关齐 | **行动**：只讲机制；**未生产压测**不报加速比 |
| Server「约 1 万行」 | ⚠仍薄 | `02` + **`14` 路径核实** | **行动**：LOC/PR **待补**（见 `15` E4）；不编 before/after |
| 5000+ 行拆解 | ⚠仍薄 | `02` + **`14` 口述模板** | **行动**：按模块+测试口述；占比 **待 cloc**（`15` E4） |
| 结构化 TPOT A/B | ⚠仍薄 | 红线已写「估计≠实测」 | **行动**：按 **`15` E1** checklist 实测 |
| 投机接受率曲线 | ⚠仍薄 | `01` 机制有 | **行动**：按 **`15` E2**；无曲线不报加速比 |
| Conductor P50/P99 | ⚠仍薄 | 仅目标/0.2s 超时口径 | **行动**：按 **`15` E3**；无数据只报目标 |
| 算子 8–10 题卡 | ✅已补 | `06`（12 题） | 60s×8 计时，勿重读 ops-Q&A |
| 跳槽软题 | ✅已补 | `08` | 拉力>推力；禁涨薪/加班话术 |
| **vLLM 加速配置（Q17）** | ✅已补 | 原 `05` 专题 + 本夜 **`09` 串讲卡** | 60s 按目标口述一遍 |

说明：原 05 表无单独「Q17 配置」行，但是二面零分题；本夜用 `09` 闭环，故增一行。

---

## 2 · 对照 `00` 验收（文档侧）

| 验收项 | 状态 |
|--------|------|
| 不再把 MindIE 编译缓存写成 LRU/128 现行口径 | ✅ |
| ≥4 张口述卡（Dynamo/投机/量化/Profiling）→ `01` | ✅ |
| ≥1 份简历第三层 → `02` | ✅ |
| interview-review README 链到本夜 | ✅（含 `09`/`10`） |
| 工作区可见文档 diff（commit 听指令） | ⚠ 未 commit（按指令） |
| PD handoff / 跳槽对练 → `07`/`08` | ✅ |
| 薄弱自检补洞 → **本文件 `10`** | ✅ 清单已出；⚠ 实测项仍薄 |

---

## 3 · 仍薄项优先级（测 / 背分开）

**明早前只能「背」的（零依赖环境）**

1. Mooncake 三层 60s（Conductor 调度 / Store 存 / TE 传）
2. `09` 按目标选配置 60s + 快问抽 5
3. Server / 5000+：**承认 LOC 待补** 的诚实句（见 `02`）

**必须补测才翻 ✅ 的（行动项，别假装有数）**

| 优先级 | 测什么 | 过关标准 |
|--------|--------|----------|
| P0 | 结构化输出 **TPOT A/B** | 有约束 vs 无约束同负载；只报实测区间 |
| P0 | 投机 **接受率–并发曲线**（或至少 1 点低并发 + 1 点大 batch） | 能指着曲线说「为何大 batch 失效」 |
| P1 | Conductor 查询 **P50/P99** | 有数才说数；否则只报 0.2s 超时目标 |
| P1 | Server / xgrammar 相关 **cloc** | 模块拆解 + 行数区间，禁拍脑袋「1 万行」 |

---

## 4 · 明早 8:00 前建议刷题顺序（45 分钟）

| 分钟 | 做什么 | 验收 |
|------|--------|------|
| 0–3 | `03` 红线 8 条默背 | FIFO/100；TTFT=测算；MTP 硬互斥 |
| 3–12 | `01`：A cost 白板 + B 拒绝采样四步 + C 决策树口诀 | 各不卡壳 1 遍 |
| 12–18 | `01` D Profiling 树 → 画到「GPU 空/满」 | 能口述分叉 |
| 18–26 | **`09` vLLM**：60s 按目标 + 盖表抽 5 快问 | Q17 开口四层不漏 |
| 26–32 | `06` 算子抽 6 题（45s/题） | Prefill/Decode、FA、CG 必中 |
| 32–38 | `07` PD 60s + `08` 跳槽 90s | D 不注册；拉力>推力 |
| 38–43 | ⚠ 项口头过：Mooncake 三层；Server/5000+ 诚实句；Spec/LMCache「未压测」 | 不报假数 |
| 43–45 | `05` 自检表扫一眼：未 ✅ 只留「测 TPOT / 投机曲线 / Conductor 延迟」 | 上场只承诺已有证据 |

**原则**：45 分钟只闭环「能开口 + 不踩红线」；测数项写进会后 TODO，不上场编百分点。
