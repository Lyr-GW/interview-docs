# 67 · 通宵迭代中场摘要 · 给明早的你

> **写给 2026-07-15 早上的自己**（本夜截点 ≈05:43；前一截点 05:40 / 05:35）  
> **一页够用**：本夜做了什么 → 强制入口 → 仍薄是什么 → **不要新开长文**。  
> **唯一入口页**：[`82`](./82-终局导航-只看这页.md)（现在睡 / 现在练 / 明早起）。  
> **睡眠优先**：[`83`](./83-睡眠优先声明.md)。  
> 导航母本：[`20`](./20-明早上场作战手册.md) · 门禁 [`41`](./41-本夜质量门禁清单.md) · 收官时间盒 [`49`](./49-通宵收官导航-睡前与起床.md) · 睡前钩 [`58`](./58-睡前最后10分钟检查单.md) · 晨间表 [`59`](./59-晨间启动清单-7点版.md)。  
> **临场 FAQ**：[`74`](./74-本夜FAQ收口30问.md) · **git 说明（未 commit）**：[`75`](./75-待用户确认-git提交说明.md)。  
> **ASCII 白板 / 停机勾选**：[`78`](./78-面试链路ASCII白板图.md) · [`79`](./79-08点停机前检查单.md)。  
> **录音跟读 / 统计快照**：[`80`](./80-可录音口述脚本合集.md) · [`81`](./81-通宵产出统计快照.md)。

---

## 0 · 三句话（先读这三句）

1. **本夜文档侧已闭环**：口径地雷消完（`40`+`70` 复扫干净）、口述/快问/收官/FAQ/录音/终局导航齐；**唯一开放环 = 实测**（见 `15`）。  
2. **强制入口不可跳**：不知翻哪 → [`82`](./82-终局导航-只看这页.md)；否则 `20` → `03` → `37`（再抽失分区 / 刷卷 / `74`；可选跟读 `80`）。  
3. **禁止**：醒来开 `interview-review/` 长文、开新专题、编 LOC/加速比/客户 raw log；**勿擅自 git commit**（见 `75`）；堆卡见 [`83`](./83-睡眠优先声明.md)。

---

## 1 · 本夜做了什么（数量级）

| 类别 | 约数 | 代表卡号 |
|------|------|----------|
| **本目录文件** | **00–83**（**84** 份 · `ls`/`wc` 核实） | 计划/索引/门禁/收官 + 口述/快问/卷/FAQ/白板/录音/终局 |
| **口述闭环卡** | **~48–52 张量级** | `01` 四面、`07`/`09`/`11`–`13`/`16`–`19`、`22`–`39`、`44`–`48`、`60`–`66`、`80` 等 |
| **快问题库** | **~180+ 题** | `05`~108 + `43`~40 + `51`~30；旁链 `48`×14、`06`×12；收口 `74`×30 |
| **抽签卷** | **45 题** | `56` A + `57` B + `68` C |
| **导航 / 门禁** | 齐 | `20` · `41` · `49`/`58`/`59` · `40`/`70` 口径 · `81` 统计 · **`82`/`83`** |
| **数字 / 白板 / STAR** | 齐 | `50` · `52` · `53` · `72`；禁语扩 `69`；计时 `73` |

**后半夜刀口**：投机 `60`/`61` → 亲和 `62`/`63` → Mooncake `64`/`65` → 对照 `66` → 摘要 `67` → 卷 C/`69` → `70`/`71` → STAR/`73` → `74`/`75` → `76`/`77` → `78`/`79` → `80`/`81` → **05:43 `82` 终局导航 + `83` 睡眠优先（未 commit）**。

**未做（故意）**：真机勾选 `15`；**commit**（说明见 `75`）；LOC/cloc；新压测加速比；长上下文/Multi-LoRA 等百科深挖（见 `71`，不上场阻塞）。

---

## 2 · 强制入口（钉死，与 `82`/`49`/`59` 一致）

| 序 | 翻 | 做什么 |
|----|-----|--------|
| 0 | [`82`](./82-终局导航-只看这页.md) | **先选岔路**（睡 / 练 / 起） |
| 1 | [`20`](./20-明早上场作战手册.md) | 翻卡总表 + 禁语扫一眼（扩表 `69`） |
| 2 | [`03`](./03-口径红线速查卡.md) | 8 红线默背（FIFO/100 必出） |
| 3 | [`37`](./37-三分钟自我介绍定稿.md) | 90s 出声 1 遍（或跟读 [`80`](./80-可录音口述脚本合集.md) §1） |
| 然后 | 失分区 / 刷卷 / FAQ | `01`B/`32`/`09`/`13`/`08`；或 `56`/`68` 抽 5；或闭卷 [`74`](./74-本夜FAQ收口30问.md) |

**7:00 整点**：优先执行 [`59`](./59-晨间启动清单-7点版.md)；有余量 [`73`](./73-对练计时器剧本-15分钟版.md) / [`80`](./80-可录音口述脚本合集.md)。  
**关灯前若还醒**：只走 [`58`](./58-睡前最后10分钟检查单.md)，不学新知识；声明见 [`83`](./83-睡眠优先声明.md)；量级一眼翻 [`81`](./81-通宵产出统计快照.md)。

口令三词：**测算 · 待补 · 未压测**。

---

## 3 · 质量门禁状态（对照 `41`）

| 面 | 状态 | 说明 |
|----|------|------|
| 文档侧验收（`00` §5） | ✅ **通过** | FIFO 现行口径、口述/简历/README 链、收官/FAQ/录音/终局齐 |
| 禁语护栏（对照 `20`/`03`） | ✅ **到位** | `40` 高危已修；**`70` 第二轮现行主语 0（干净）** |
| **仍薄** | ⚠ **仅实测** | 一律链 [`15`](./15-实测补洞作业单.md) |
| 可选加深 | 不阻塞 | [`71`](./71-仍可加深但不阻塞清单.md)；被问用 `39` 挡 |
| git | ⏸ **待确认** | 范围/message 见 [`75`](./75-待用户确认-git提交说明.md)；**未执行** |

**结论**：明早任务 = **开口与红线**，不是补文档；测数项无环境就说「待补」。下一步优先：**睡 → 刷 59 → 听令再 commit**（见 `81`/`83`）。

---

## 4 · 明早禁止清单（写给你看的）

| 禁止 | 改做 |
|------|------|
| 新开长文 / 新专题 / 重读 `sglang/12` 全文 | 只翻本夜卡号；深文当字典；FAQ 用 `74`；跟读用 `80`；入口用 `82` |
| 同夜再扩「百科卡」 | 时间盒见 `49`/`82`；非阻塞主题见 `71`；声明见 `83` |
| 无曲线报加速比；客户 raw log 说 −70% | 测算 / 机制句 |
| 拍脑袋「约一万行」 | 「LOC 待补」+ 模块拆（`14`） |
| 一醒来先 git commit / 整理 diff | 先读 `75`；**听指令**再暂存 |

---

## 5 · 失分区四母卡（平安二面）

| 伤 | 60s 锚 | 翻 |
|----|--------|-----|
| 投机 | 拒绝采样四步 + EAGLE→MTP→DSpark | `01`B / `32` / `34`；点名 `60`/`61`；FAQ `74` C；录音 `80`§5 |
| 配置 | 按目标选配置，忌背清单 | `09`（+`11`）；录音 `80`§6 |
| Mooncake | 三层 + Connector≠官方 | `13`；边界 `36`；对照 `66`；成本 `64`；录音 `80`§4 |
| 跳槽 | 拉力>推力 | `08`；录音 `80`§7 |

全场计时对练（有余量）：[`42`](./42-45分钟模拟面试串联剧本.md)；15′ 压缩：[`73`](./73-对练计时器剧本-15分钟版.md)。

---

## 6 · 一页抄写版（可贴显示器）

```text
入口: 82三岔 · 或 20 → 03 → 37 → 失分区/56/68/74（跟读80）
仍薄: 仅 15 实测（TPOT/投机曲线/Conductor/cloc）
门禁: 文档✅ 禁语✅ 口径复扫✅(70) 开放环=测数
题库: ~180+ 快问；卷 45；口述 ~50；FAQ 30；文件 84
禁: 新长文 / 编数 / 客户日志当 −70% / 擅自 commit
口令: 测算 · 待补 · 未压测
睡前58 / 晨间59/73/80 / 总表49 / 上场20 / 禁语69 / FAQ74
统计81 / 终局82 / 睡眠83 / 非阻塞71 / git 说明75
```

---

## 验收（本页自身）

- [x] 链 `20`/`41`/`49`/`58`/`59`；挂 `70`–`83`；统计量级（**84** 份）  
- [x] 约 80–110 行；明确「不要新开长文」；**未 commit**  
