# 69 · 禁语 20 条默背卡（绝对不要说）

> **本夜续批**（2026-07-15 · 04:54）  
> 用途：把 [`20`](./20-明早上场作战手册.md) §5 的 10 条「绝对不要说」**扩到 20 条**；每条给 **正确替换半句**，上场默背改口。  
> 母本：`20` §5 + `03` 八条 + 附加禁语；刀口旁链 `62`–`66`/`61`/`14`。  
> 用法：出门前出声扫一遍（≈2′）；踩雷当场用「❌→✅」半句重答。  
> 睡前钩仍 [`58`](./58-睡前最后10分钟检查单.md)；晨间禁语 [`59`](./59-晨间启动清单-7点版.md) §3。

---

## 0 · 怎么用（20 秒）

1. 先背 **#1–#10**（与 `20`/`03` 对齐），再背 **#11–#20**（本夜后半刀口）。  
2. 每条只记：**禁出口半句 → 改口半句**；锚卡号仅对完后翻。  
3. 被抓包改口框架仍走 [`39`](./39-高压追问应急话术卡.md)，本卡不替代高压全文。

**口令骨架**（串起来）：

```text
100 FIFO → 测算非日志 → torch 组合 → MTP 互斥 → D 不注册
→ 不吹 kernel → 不编 LOC → 不报未压测% → 跳槽非涨薪
→ Connector≠三层 → 超时≠P99 → Store≠Conductor删 → DSpark≠我测
```

---

## 1 · 绝对不要说 × 正确替换（20）

| # | ❌ 绝对不要说 | ✅ 正确替换（半句） | 锚 |
|---|---------------|-------------------|-----|
| 1 | 编译缓存是 **LRU / 128** | **FIFO / 100**，命中不调序 | `03` #1 |
| 2 | TTFT−70% 是 **客户 raw log / 实测** | **代表性测算**，可白板公式与假设 | `03` #7 · `38` |
| 3 | 我们线上有 **guidance** | 唯一落地 **xgrammar**；guidance=预留 | `03` #8 |
| 4 | **D 实例也注册** Conductor | D **不注册**；仅 P/U | `03` #6 · `07` |
| 5 | bitmask 是 **自研 fused / AscendC** | **torch 组合** `masked_fill_`；fused 是优化点 | `03` #3 · `19` |
| 6 | 编造 **LOC /「删了 1 万行」精确数** | 路径+模块拆解；数字 **待 cloc** | `14` · `15` E4 |
| 7 | 编造 **投机 / Spec / LMCache 加速比** | 只讲机制；**无压测不报百分点** | `01` B · `04` |
| 8 | MTP 与结构化 **可以一起开** | **入口硬互斥**；插件未联调 | `03` #5 · `23` |
| 9 | 跳槽因 **涨薪 / 嫌加班 / 跟风** | **拉力>推力**；方向收窄；势能最好时换 | `08` |
| 10 | 「我最擅长做得不多」或 **谎称生产 kernel/HCCL** | 点 SO+亲和；算子/通信=**协作归因** | `08` · `36` |
| 11 | **Connector 是 Mooncake 官方三层之一** | Connector=引擎**适配层**；官方三层=Conductor/Store/TE | `13` · `66` |
| 12 | 把 **现状树行数** 冒充重构删行 | 只报核实路径；删行数 **待补** | `14` · `35` |
| 13 | 把 **Tool Call 行数** 混进 SO「5000+」 | 5000+=结构化交付边界；FC 另账 | `02` · `28` |
| 14 | 「Conductor **P99=200ms** / 已测通过」 | **200ms=客户端配置超时**；P50/P99 是**目标·待补** | `62` · `50` |
| 15 | 「超时回退到 **轮询 RR**」 | 回退 **LoadBalance（最小负载）**；再失败才 RR | `62` |
| 16 | 「Conductor 负责把 **冷 KV 删掉**」 | **Store** `BatchEvict` 释空间；Conductor 最多丢索引 | `65` |
| 17 | 「vLLM **还要外挂** 才能用 Mooncake」 | `main` **已原生注册** connector；策略深度另说 | `66` |
| 18 | 「SGLang CacheAware=**查 Mooncake 元数据**」 | 近似前缀树+负载；**不直查** Store；精确查=Motor→Conductor | `66` · `26` |
| 19 | 「DSpark **我们线上测到** +x%」 | 公开报告相对 MTP-1；**非本仓实测** | `61` · `02` |
| 20 | 「Motor 打分里有完整 **Ttransfer 成本模型**」 | Motor **简化**（credit 折抵）；**Conductor 精细**含传输账 | `64` · `63` |

---

## 2 · 分区默背（可选，按场景）

| 场景 | 先背号 | 半句钩子 |
|------|--------|----------|
| 开场 / 自我介绍 | 1–2 · 5 · 9–10 | FIFO · 测算 · 不吹 kernel · 拉力 |
| 结构化 / MTP | 3 · 5 · 8 · 13 | xgrammar · torch · 互斥 · 行数边界 |
| 亲和 / 超时 | 4 · 14–15 · 20 | D 不注册 · 配置超时 · LB ≠ RR · Motor 简化 |
| Mooncake / 引擎 | 11 · 16–18 | 适配层 · Store 逐 · 原生≠流式 · 猜 vs 查 |
| 投机 / 数字 | 7 · 19 · 6 · 12 | 无曲线不报% · 公开非实测 · 待 cloc |

---

## 3 · 30 秒自检（出门前）

出声勾：

- [ ] #1 FIFO/100  
- [ ] #2 测算非日志  
- [ ] #8 MTP 互斥  
- [ ] #4 D 不注册  
- [ ] #5 torch 组合  
- [ ] #11 Connector≠三层  
- [ ] #14 200ms=配置  
- [ ] #19 DSpark≠我测  

全过 → 出门；任一卡壳 → 只重念该行 ✅ 半句，不读深文。

---

## 4 · 与 `20` / `03` / `58` / `59`

| 文档 | 本卡怎么用 |
|------|------------|
| [`20`](./20-明早上场作战手册.md) §5 | #1–#10 同源；本卡多 #11–#20 |
| [`03`](./03-口径红线速查卡.md) | 八条红线母本；本卡是开口禁语扩表 |
| [`58`](./58-睡前最后10分钟检查单.md) | 睡前仍扫短口令；不必满刷 20 |
| [`59`](./59-晨间启动清单-7点版.md) | 7:55 可改扫本卡 §3 八勾 |

```text
20 §5（10）──扩──→ 本卡（20）
踩雷改口 → 39；红线母本 → 03
```

---

**本卡完** · 手册 [`20`](./20-明早上场作战手册.md) · 红线 [`03`](./03-口径红线速查卡.md) · 高压 [`39`](./39-高压追问应急话术卡.md) · 卷 C [`68`](./68-随机抽题卷C.md) · 索引 [`21`](./21-本夜产出索引.md)
