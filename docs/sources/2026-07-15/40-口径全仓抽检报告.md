# 40 · 口径全仓抽检报告（MindIE 编译缓存）

> **时间**：2026-07-15 ≈03:30（UTC+8）  
> **范围**：`docs/` 全仓 grep（MindIE **Schema 编译缓存** × LRU/128）  
> **正确现行口径**：`SHA-256(key)` + **FIFO / 默认 100**（命中不调序）  
> **排除规则**：句中明确为「禁止 / 勿说 / 禁 / 旧口径 / 风险对照 / 改口 / 不是 LRU」的，计为**对照句**，不记「需修」  
> **非目标**：引擎 PrefixCache / Store / 近似树 / SGLang Radix 的 LRU（另一层机制）

---

## 1 · 扫描方法

| 步 | 动作 |
|----|------|
| 1 | `rg`：`LRU.*128` / `128.*LRU` / `容量.?128` / `编译缓存.*LRU` / `SHA-256.*LRU` / `+ LRU` |
| 2 | 人工过滤：非编译缓存（FA M=128、block_size、引擎 LRU 等）丢弃 |
| 3 | 余下按三类打标：**已修** / **需修** / **可保留（历史）** |
| 4 | 需修最高危 **直接改 1–3 处**（本批已改，见 §4） |

---

## 2 · 结论摘要

| 结论 | 说明 |
|------|------|
| 上场导航区 | `2026-07-15/03`·`20`·`37`·`01`·`05` 等均为 **FIFO/100 正说 + LRU/128 禁语对照** → **已修** |
| 深文主线 | `interview-review/03`·`08`·`16`·`18`，`suanzi/09`·`15`·`24` 等已钉代码真相 → **已修** |
| 残留风险 | 复盘/题干/串线文里仍有「LRU/128」作**叙述主语**、未标历史 → 本批已修 3 处 |
| 历史可留 | 二面当日答法、模拟题干「设问用旧口径」→ **可保留**，但旁注「勿当现行」 |

**一句话**：文档侧「把 LRU/128 当现行口径」的高危句已清；剩余命中多为禁语表或历史记录。

---

## 3 · 命中清单（编译缓存相关）

### 3.1 已修（正说 FIFO/100，或禁语对照）

| 文件 | 角色 | 建议 |
|------|------|------|
| `2026-07-15/03` · `20` · `37` · `01` · `05` · `02` · `10` | 红线 / 作战 / 开场 / 口述 | **已修** — 上场以这些为准 |
| `interview-review/03` · `08` · `16` · `18` | 深文 + 模拟实录 | **已修** — ⚠ 代码真相块齐全 |
| `suanzi/09` · `15` · `24` · `08` · `10` · `13` · `21` 等 | 算子答辩包 | **已修** — 「勿说 LRU」 |
| `00` §5 验收勾选 | 计划风险句保留 LRU 字样作对照 | **可保留**（风险描述，非现行） |

### 3.2 本批由「需修」→「已修」（最高危 3 处）

| # | 文件 | 原问题 | 处理 |
|---|------|--------|------|
| 1 | `interview-review/17` §4.1 | 「简历上的 SHA-256 + **LRU** 编译缓存」作现行主语 | → 改为 **FIFO/100**，并写明禁 LRU/128 |
| 2 | `interview-review/01` Q33–Q34 + §分析 Q31–34 | 复盘正文把 LRU/128 当「加分设计」复述，易当现行 | → 标 **历史口述**；分析段钉现行 FIFO/100 |
| 3 | `2026-07-06/01` Q14 题干 | 「LRU 容量 128 怎么定」无订正提示 | → 题干加 ⚠：答时先订正 FIFO/100 |

### 3.3 可保留（历史记录 / 对照 / 非现行）

| 文件 | 为何可留 | 上场注意 |
|------|----------|----------|
| `interview-review/01` Q33–34 原文答法 | 二面实录，已加 ⚠ 旁注 | 只作「当日说了什么」，开口用 FIFO/100 |
| `2026-07-06/01` Q14（改后） | 模拟追问故意用旧简历数字 | 先订正再展开 |
| `2026-07-06/03` Q14 参考答 | 已写「不是 128 / 不是 LRU」 | 可作话术母本 |
| `00`「最大风险：LRU/128 vs FIFO/100」 | 计划风险对照 | 勿念成现行 |
| `20`/`37`/`03` 禁语列「LRU/128」 | 禁语表 | 对照用 |

### 3.4 扫到但排除（非 MindIE 编译缓存）

引擎 `ref_cnt`+LRU、Mooncake Store near-LRU、SGLang Radix 驱逐、llm-d approx LRU、FA `M=128` / `block_size=128` 等 —— **不在本抽检「需修」范围**。

---

## 4 · 本批直接修补记录

| 时间 | 文件 | Diff 意图 |
|------|------|-----------|
| 03:30 | `interview-review/17-…综合专题.md` | §4.1 现行口径 → FIFO/100 |
| 03:30 | `interview-review/01-面试复盘总结.md` | Q33/34 + 分析段：历史 vs 现行 |
| 03:30 | `2026-07-06/01-模拟面试问题清单.md` | Q14 题干加订正提示 |

未改（故意）：`00` 风险句、各卡「禁语」列、二面答法原文（保留证据链）。

---

## 5 · 残留与明早抽查

| 优先级 | 动作 |
|--------|------|
| P0 | 开口只背 `03`：FIFO/100；被甩旧简历立即改口 |
| P1 | 若再翻 `ir/01` / `ir/17`，确认旁注仍在 |
| P2 | 可选：对 `2026-07-06/02`「手撕 LRU 呼应编译缓存」加半句「实现是 FIFO，手撕 LRU 是算法题」——**非本夜必做** |

**抽检闭环**：本报告 + `41` 门禁；索引已挂 `21` / README / `00`。

---

## 6 · 一页口诀

> MindIE Schema 编译缓存 = **SHA-256 + FIFO≈100，命中不调序**。  
> 听到「LRU/128」→ 承认旧口述/旧简历 → **立刻改口代码真相**。  
> 文档里再看到 LRU/128：先看是否「禁/旧/风险」；若当现行主语 → 按本报告 §3.2 同级修法。
