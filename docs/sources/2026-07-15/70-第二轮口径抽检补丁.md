# 70 · 第二轮口径抽检补丁（MindIE 编译缓存）

> **时间**：2026-07-15 ≈05:10（UTC+8）  
> **相对**：[`40`](./40-口径全仓抽检报告.md)（03:30 第一轮；高危 3 处已修）  
> **正确现行口径**：`SHA-256(key)` + **FIFO / 默认 100**（命中不调序）  
> **本轮目标**：再扫一遍「MindIE 编译缓存 = LRU/128」是否仍作**现行主语**；有则修 ≤3 处；无则留**干净证据**。

---

## 1 · 结论（先读）

| 项 | 结果 |
|----|------|
| **需修（现行主语）** | **0 处** |
| **本轮直接改文** | **无**（未动正文） |
| **判定** | **干净** — 残留命中均为禁/旧/对照/题干/抽检元叙述 |
| **上场口令** | 仍只背 `03`：FIFO/100；被甩旧简历立刻改口 |

**一句话**：第二轮确认文档侧无「把 LRU/128 当 MindIE Schema 编译缓存现行设计」的句子；可上场，不因口径文档阻塞。

---

## 2 · 扫描命令与命中说明

### 2.1 命令（可复跑）

```bash
# 在仓库根目录
rg -n 'LRU.?/?128' docs/ --glob '*.md'          # 宽扫：约 48 行量级
rg -n '编译缓存.*LRU|LRU.*编译缓存' docs/ --glob '*.md'
rg -n 'LRU.*128|128.*LRU|容量.?128|编译缓存.*LRU|SHA-256.*LRU' docs/ --glob '*.md'
```

### 2.2 人工过滤规则（与 `40` 一致）

| 类别 | 处置 |
|------|------|
| 禁语 / 勿说 / 不是 LRU / 非 LRU / 改口 | **对照** → 可保留 |
| 旧口述 / 历史加分叙事 / ⚠ 旁注 | **历史** → 可保留 |
| 计划「最大风险：LRU/128 vs FIFO/100」 | **风险对照** → 可保留（勿念成现行） |
| 模拟题干故意用旧数字 + 已标先订正 | **题干** → 可保留 |
| FA `M=128`、block_size、引擎/Radix/Store near-LRU | **非编译缓存** → 排除 |
| 无标记地把 LRU/128 写成现行设计 | **需修** → 本轮 **0** |

### 2.3 可疑筛（排除禁/旧/对照后）

对「编译缓存…LRU」「LRU/128 容量」等再筛一遍；过滤「禁|勿|不是|旧|历史|风险|对照|错答|题干|改口|禁语」后，**剩余仅为**：

| 命中 | 为何不算现行主语 |
|------|------------------|
| `20` 禁语表行「编译缓存是 LRU/128」 | **禁语列**（正确列是 FIFO/100） |
| `40` 自身写 grep 模式 / P2 备注 | **抽检元文档** |
| `69`/`58`/`03` 等禁语表 | **对照用** |

→ **无「需修」行**。

---

## 3 · 与第一轮衔接

| 轮次 | 时间 | 动作 |
|------|------|------|
| `40` | 03:30 | 修 `ir/17` · `ir/01` · `07-06/01` 三处高危现行主语 |
| **本页 `70`** | **05:10** | **复扫干净**；本轮 **0 处需修** |

**可选加固（仍非阻塞，与 `40` §5 P2 同）**：`2026-07-06/02` Q50「手撕 LRU 呼应编译缓存」——可加半句「实现是 FIFO，手撕是算法题」。**本夜不改**；被问到口述订正即可。

---

## 4 · 命中分类速览（干净证据）

| 桶 | 代表 | 现行主语？ |
|----|------|------------|
| 上场禁语 | `03`/`20`/`37`/`69`/`58` | 否 |
| 正说 FIFO | `ir/03`·`08`·`16`·`17`·`18`，`suanzi/09`·`24` | 否（已钉 FIFO） |
| 历史/改口 | `ir/01` Q33–34 ⚠ | 否 |
| 风险/计划 | `00`「最大风险」句 | 否 |
| 抽检自指 | `40`/`41`/`21` README 链 | 否 |
| 题干设问 | `56` R1、「LRU 还是 FIFO？」 | 否（要答 FIFO） |

---

## 5 · 上场抽查（30 秒）

1. 开口：编译缓存？→ **FIFO / 100，命中不调序**  
2. 被甩「简历写过 LRU 128」？→ 「旧口述；代码是 FIFO/100」  
3. 勿把 Radix/Store **LRU** 与 SO **编译缓存** 串层（见 `54`/`50`）

---

## 6 · 验收

- [x] 复扫 `docs/`；现行主语 **0**；本轮 **未改文**（干净证据已记）  
- [x] 链 `40`；索引挂 `21` / README / `00` / `67`  
- [x] 约 60–100 行；**未 commit**
