# 77 · 代码锚点速查 20 条

> **本夜续批**（2026-07-15 · 05:19）  
> 格式：**文件/符号 → 一句话用途 → 面试怎么提**。  
> 优先已核实：MindIE-LLM `structured_output_*`、`infer_param` MTP、Motor 亲和路径；找不到标 **待补**。  
> 链：[`14`](./14-Server重构与5000行证据提纲.md) · [`23`](./23-MTP与结构化互斥深挖卡.md) · [`19`](./19-bitmask-NPU路径诚实卡.md) · interview-review `12`。  
> 路径相对工作区根；行号会漂，以符号名为准。

---

## A · 结构化输出（MindIE-LLM）

| # | 文件 / 符号 | 一句话用途 | 面试怎么提 |
|---|-------------|------------|------------|
| 1 | `MindIE-LLM/.../structured_output/structured_output_manager.py` · `StructuredOutputManager` | SO 总控：编译缓存、GrammarBackend、bitmask 编排、PD sync | 「主干在 Manager；缓存 **FIFO/100**，不是 LRU/128」→ `03`/`14` |
| 2 | 同文件 · `GuidedDecodingBackendType` / `_DEFAULT_GRAMMAR_CACHE_SIZE` | 线上后端枚举；默认缓存容量 100 | 「线上仅 **xgrammar**；guidance=预留」→ `02` |
| 3 | `.../structured_output_grammar.py` · `XgrammarGrammar` / `StructuredOutputGrammar` | Schema→grammar→matcher；accept 推进 tried 游标 | 「约束在采样前改 logits；parser 是后处理，正交」→ `28` |
| 4 | `.../structured_output_bitmask.py` · `apply_token_bitmask_inplace` / `_npu` | 把 bitmask 打到 logits；NPU 走 torch 组合 | 「**不是**自研 AscendC fused；热路径经验 <1%–3% TPOT」→ `19` |
| 5 | `.../samplers/logits_handlers/pta_handlers.py` · `@register_class("guided_decoding")` | 采样器挂载 guided bitmask | 「从插件到 logits handler 一条链可指路径」→ `14` |
| 6 | `.../plugins/plugin_manager.py` · `_init_structured_output_manager` | 插件生命周期挂 SO manager | 「异步分支若时序错会对 mask」→ `16` |

---

## B · MTP × 契约互斥

| # | 文件 / 符号 | 一句话用途 | 面试怎么提 |
|---|-------------|------------|------------|
| 7 | `MindIE-LLM/src/server/endpoint/utils/infer_param.cpp` · `InferParam::ValidateMtpConstraints` | mtp 开且带结构化 `response_format` → 硬报错 | 「**入口硬互斥**；原文 `cannot be used with mtp`」→ `23` |
| 8 | `infer_param.h` · `ValidationContext::mtpEnabled` | Serving 侧 MTP 开关上下文 | 「互斥在 C++ 契约层，不是只靠 Python 约定」 |
| 9 | `tests/dlt/ut/server/utils/test_infer_param.cpp`（~MTP×SO） | UT：text 可开 MTP；structured 必须拦 | 「有单测钉死契约，不是口头规范」 |
| 10 | `.../plugins/mtp/mtp_plugin.py` · `MtpPlugin` | MTP 候选前向插件 | 「插件层与 grammar **零挂载**；同开不安全」→ `23`/`60` |
| 11 | `.../plugins/mtp/decoding_policy.py` · `DecodingPolicy` | MTP propose/verify 策略骨架 | 「verify 偏贪心比对；勿直接等同 vLLM 拒绝采样」→ `34` |

---

## C · Motor KV 亲和

| # | 文件 / 符号 | 一句话用途 | 面试怎么提 |
|---|-------------|------------|------------|
| 12 | `MindIE-PyMotor/.../policy/kv_cache_affinity.py` · `KvCacheAffinityPolicy` | 查 Conductor 索引 + 打分选实例（precise） | 「**查**缓存不是猜树；token 级」→ `12`/`26` |
| 13 | 同文件 · `_select_unified` / `_select_load_gated` | unified 融分 vs load_gated 先负载再亲和 | 「防 herding：overlay→top-k→全局重排」→ `17`/`63` |
| 14 | 同文件 · `TokenizerManager.apply_chat_template(..., tools=)` | 与引擎同源 tokenize；**tools 必须透传** | 「字符级 router 对不上 hash；fail-closed」→ `18` |
| 15 | `.../api_client/conductor_api_client.py` · `ConductorApiClient` | 查/注册 Conductor；超时回退 | 「超时约 **0.2s**→LoadBalance，失败不拖垮」→ `62` |
| 16 | `.../policy/load_balance.py` · `LoadBalancePolicy` | 亲和失败/超时的回退策略 | 「回退仍可服务，不只亲和一条腿」 |
| 17 | `.../policy/factory.py` · `create` / `SchedulerType.KV_CACHE_AFFINITY` | 策略工厂注册亲和/LB/RR | 「调度策略可插拔，OCP」 |

---

## D · Mooncake / vLLM 对照锚（抽查）

| # | 文件 / 符号 | 一句话用途 | 面试怎么提 |
|---|-------------|------------|------------|
| 18 | `vllm/.../kv_connector/factory.py` · `"MooncakeConnector"` / `"MooncakeStoreConnector"` | vLLM 原生注册 Mooncake 系 Connector | 「Connector=引擎适配层，**不是** Mooncake 官方三层之一」→ `13`/`66` |
| 19 | `vllm/vllm/config/cache.py` · `enable_prefix_caching`；`scheduler.py` · `enable_chunked_prefill` | 配置真相入口（V1 常默开） | 「按目标讲旋钮，不背名单」→ `09` |
| 20 | Mooncake Conductor/Store/TE **源码入口聚合** | 设计文档在 `Mooncake/docs/source/design/{conductor,transfer-engine}`；Store 在 `mooncake-store/` | 「面试讲三层职责即可；深挖 Master/Client 翻 `65`」· 更细 C++ 符号 **待补**（本夜未钉单文件 class） |

---

## 旁证（不占 20 条名额 · 可指）

| 路径 | 一句 | 何时提 |
|------|------|--------|
| `MindIE-LLM/.../plugins/structured_output/__init__.py` | 导出 `apply_token_bitmask_inplace` 等公开符号 | 被问「入口包名」 |
| `MindIE-LLM/.../utils/tg_infer_context_store.py` · `set_structured_output_manager` | batch 上下文挂 SO manager | 被问「manager 挂哪」 |
| `MindIE-LLM/src/server/endpoint/single_req_infer_interface_base.cpp` · `ctx.mtpEnabled = serverConfig.mtpEnabled` | 请求上下文灌 MTP 开关 | 追问互斥从哪读配置 |
| `motor/config/config_utils.py` · `KV_EVENTS_CONFIG = "kv-events-config"` | 引擎 kv-events 配置键常量 | 假命中/ZMQ 配置侧 → `25` |
| `vllm/.../kv_connector/v1/mooncake/store/connector.py` · `MooncakeStoreConnector` | Store 连接器（非 P2P Connector） | 对照 PD 直传 vs 池化 → `66` |

---

## 待补（找得到主题、未钉死符号）

| 主题 | 状态 | 备注 |
|------|------|------|
| Motor 侧 ZMQ `BlockStored`/`BlockRemoved` **消费类名** | **待补** | 配置键 `kv-events-config` 已核实（`motor/config/config_utils.py`）；事件消费类路径未在本表钉死 → 机制仍翻 `25` |
| Mooncake Store `near-LRU` / soft pin **C++ 符号** | **待补** | 口述口径见 `65`；本夜未指向具体 `.cc` |
| HTTP Server 重构「5000+/约1万」**author cloc** | **待补** | 路径提纲见 `14`/`35`；勿编行数 |
| bitmask NPU fused kernel PR | **无** | 诚实：torch 组合；勿吹自研 |
| `router/` 字符级 radix（竞品对照） | 已有深文 | `router/src/tree.rs` · `DashMap<char, …>`；本表不占条，翻 ir/15 |

---

## 60s 用法

1. 简历挖 SO → 开口锚 **#1+#4+#7**（Manager / bitmask / 互斥）。  
2. 挖亲和 → **#12+#14+#15**（查索引 / tools / 超时回退）。  
3. 挖 Mooncake → **#18 + `13` 三层**；深 Store 再 `65`。  
4. 被追「指代码」→ 只报本表已核实行；**待补**主题改「机制我熟，符号我回去钉」。  
5. 被追「有没有 PR/行数」→ 路径可指，**author cloc 待补**，勿报假数。

**三句防穿帮**

> 「互斥在 `ValidateMtpConstraints`，不是口头约定。」  
> 「亲和是 `KvCacheAffinityPolicy` 查 Conductor，不是本地近似树。」  
> 「bitmask NPU 是 torch 组合，不是自研 fused kernel。」

---

## 交叉引用

| 文档 | 用途 |
|------|------|
| [`14`](./14-Server重构与5000行证据提纲.md) | SO/Server 证据母本 |
| [`23`](./23-MTP与结构化互斥深挖卡.md) | infer_param 互斥 |
| [`19`](./19-bitmask-NPU路径诚实卡.md) | bitmask 诚实 |
| [`12`](./12-假命中与驱逐感知口述卡.md) / [`17`](./17-Herding与负载门控口述卡.md) | Motor 行为 |
| [`76`](./76-失分区15分钟速成课.md) | 失分区口述专训（机制侧） |
| [`50`](./50-简历数字诚实总表.md) | 数字性质；与路径分离 |
