# 口径红线速查卡（上场倒背）

> 来源：`2026-07-06/00`、`2026-07-10/00` §5、本夜源码核实。  
> 任何与下表冲突的旧口述/旧简历表述，**以上表为准**。

| # | 红线 | 正确口径 | 错误口径（禁止） | 代码/文档锚点 |
|---|------|----------|------------------|---------------|
| 1 | 编译缓存 | 默认 **100** 条，**FIFO**（命中不调序） | LRU / 128 | `structured_output_manager.py` L95, L1054–1057 |
| 2 | tokenizer | Coordinator **本地 model_path 同源加载** | 运行时从引擎拉取 | `12`/`04` |
| 3 | bitmask apply | **torch NPU 算子组合** `masked_fill_` | 自研 fused kernel | `structured_output_bitmask.py` |
| 4 | xgrammar | **字节级 PDA** | 仓内写「FSM」不严谨 | `03` |
| 5 | MTP × 结构化 | **入口硬互斥** | 「可以一起开」 | `infer_param.cpp`；见 `02` 追问弹药 |
| 6 | KVA 注册 | D 实例 **不注册** Conductor；仅 P/U | D 也上报索引 | `12`/`13` |
| 7 | TTFT−70% | **代表性测算**（可讲公式与假设） | 「客户原始日志实测 −70%」除非拿出日志 | `12`/`13` Round5；`02` 诚实卡 |
| 8 | guidance | MindIE **唯一落地后端 xgrammar**；多后端是扩展抽象/预留 | 「我们线上有 guidance」 | `02` §1 |

## 30 秒自检

1. 缓存？→ 100 / FIFO  
2. 70%？→ 测算，不是甩客户日志  
3. bitmask？→ torch 组合，可优化点是 fused  
4. MTP+结构化？→ 互斥  
5. D 实例？→ 不注册 Conductor  
