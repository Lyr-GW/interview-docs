# 52 · 高频白板默写纸（可打印 · 一张纸密度）

> **本夜续批**（2026-07-15 · 04:04）  
> 用途：打印 / 盖答默写——五块一张纸；默写空在前，答案折叠在后。  
> **链**：[`32`](./32-拒绝采样白板特训卡.md) · [`01` A](./01-P0口述卡-Dynamo投机量化Profiling.md) · [`38`](./38-TTFT五段分解与70%测算卡.md) · [`17`](./17-Herding与负载门控口述卡.md) · [`03`](./03-口径红线速查卡.md)。  
> 盖住 §6 → 按块默写 → 对答案；错块翻链卡 ≤5′。禁 LRU/128；−70% 标测算；无曲线不报加速比。

---

## 0 · 过关表

| # | 块 | 过关 | 加深 |
|---|-----|------|------|
| 1 | 拒绝采样 | `α=min(1,p/q)` + `norm(max(0,p−q))` | `32` |
| 2 | Dynamo cost | raw→credit→adjusted→cost→min | `01` A |
| 3 | TTFT 五段 | 五名齐；主砍=prefill | `38` |
| 4 | Motor 打分 | unified 可翻盘 / gated 不出 Top-N | `17` |
| 5 | 编译缓存 | 100 FIFO；命中不调序 | `03` #1 |

---

## 1 · 默写空 · 拒绝采样（链 `32`）

```text
① Draft：________________________________
② Verify：_______________________________
③ α = ________ ；拒后从 ________ 采样
④ 单 token ≈ ______ / ______ ；要 < ______
   大 batch 失效一句：____________________
无损一句：______________________________
```

---

## 2 · 默写空 · Dynamo cost（链 `01` A）

```text
raw_prefill = (________ + ________) / block_size
overlap_credit = L1____ + L2____ + L3____ + shared(常___)
adjusted = max(raw − credit, 0)
cost = ______ × adjusted + ______  → 选 ____
vs Motor 一句：__________________________
```

---

## 3 · 默写空 · TTFT 五段（链 `38`）

```text
TTFT = T_____ + T_____ + T_____ + T_____ + T_____
动因：tokenize__ / Conductor__ / queue__ / prefill__ / delivery__
压缩：c₀ + T_full×(1−h)
  h≈0.10 → ____ms ；h≈0.78 → ____ms ；性质：________
```

---

## 4 · 默写空 · Motor 打分（链 `17`）

```text
Herding 一句：____________________________
三版：overlay → ____ → ____（gated 钉 __）
unified：scale×max(0,isl−credit×matched)+w×load ；翻盘？__
load_gated：负载 Top-N → N 内 matched ；出集合？__
matched=6000 翻盘：load_A−load_B > ____
```

---

## 5 · 默写空 · 编译缓存 FIFO（链 `03`）

```text
容量 ____ ；策略 ____ ；命中调序？ Y/N → __
Key：________
图：淘汰端____ ；插入端____ ；命中挪位？____
禁说：________ / ________
```

```text
  淘汰→ [旧]——[ ]——[新] ←插入
            ↑命中：？
```

---

## 6 · 答案要点（折叠）

<details>
<summary><b>① 拒绝采样</b></summary>

```text
① x1..xk + q  ② target 一次前向 → 各位 p
③ α=min(1,p/q)；拒→丢后缀+norm(max(0,p−q))
④ (T_draft+T_verify)/τ < T_target
大 batch：compute-bound，verify 抢算力→吞吐常掉
无损：分布≡纯 target；≠一定更快
```

</details>

<details>
<summary><b>② Dynamo cost</b></summary>

```text
raw=(active_prefill+uncached)/bs
credit≈1.0·dev+0.75·host+0.25·disk+shared(0)
cost=prefill_load_scale×adjusted+decode_blocks → min
vs Motor：Dynamo=栈内 tier 权重；Motor=Conductor precise
```

</details>

<details>
<summary><b>③ TTFT 五段</b></summary>

```text
tokenize+Conductor+queue+prefill+delivery
固定/固定/间接/直接砍/几乎不动
1187→351；代表性测算（非客户 raw log）
```

</details>

<details>
<summary><b>④ Motor 打分</b></summary>

```text
滞后视图+同前缀 burst→齐打最优机
overlay(删)→top-k→unified(#304)；gated 钉 V2
unified 能翻盘；gated 不出最闲集合
翻盘临界 >6000（测算例）
```

</details>

<details>
<summary><b>⑤ 编译缓存</b></summary>

```text
100；FIFO；命中不调序；SHA-256(schema)
淘汰=队头；插入=队尾；命中不挪
禁：LRU / 128
```

</details>

---

## 7 · 勾选

- [ ] 拒绝采样四步+无损  - [ ] Dynamo cost 骨架  
- [ ] TTFT 五段+测算  - [ ] unified vs gated  - [ ] FIFO/100  

错≥2：翻链卡再默；勿开深文。简历翻卡→[`53`](./53-简历条目到卡号速查.md)·上场[`20`](./20-明早上场作战手册.md)
