# 71 · 仍可加深但不阻塞清单

> **时间**：2026-07-15 ≈05:10（UTC+8）  
> **用途**：列出「有空再补、**不上场阻塞**」主题；每项一句 **若被问到怎么挡**。  
> **强制入口不变**：[`20`](./20-明早上场作战手册.md) → [`03`](./03-口径红线速查卡.md) → [`37`](./37-三分钟自我介绍定稿.md)。  
> **真正开放环**仍只有实测 [`15`](./15-实测补洞作业单.md)——本清单 ≠ `15`。

---

## 0 · 怎么用（20 秒）

| 场景 | 动作 |
|------|------|
| 睡前/通宵还想写 | **停**；本页主题一律不当本夜必做 |
| 面试被点名 | 用本页「怎么挡」+ 链卡；勿编生产数字 |
| 面试后有空 | 按兴趣加深；仍勿伪造 PR/加速比 |

**口令**：边界诚实 → 同构迁移 → 反问场景（母本 [`39`](./39-高压追问应急话术卡.md)）。

---

## 1 · 主题清单（有空再补）

### 1 · 长上下文（128K+ / YaRN / 窗口外推）

| | |
|--|--|
| **现状** | 有 KV 量化/slot、传值阈值（`64`）、chunked（`44`）旁链；无独立「长上下文系统」口述卡 |
| **加深方向** | RoPE/YaRN 一句；paged KV 显存粗算；长 prefill 与 PD/chunked 选型 |
| **若被问到怎么挡** | 「长上下文我主战场是 **KV 显存与搬移成本**，不是训侧外推论文；slot/量化/传不传按带宽阈值拆——外推细节我没生产落地，按你们窗口长度我可以对齐一版预算。」→ 翻 `29` KV FP8 · `64` · `44` · 高压 `39` |

---

### 2 · Multi-LoRA / 多适配器服务

| | |
|--|--|
| **现状** | SGLang Radix 有 LoRA 命名空间旁注（`48`/sglang/12）；简历无 Multi-LoRA 交付 |
| **加深方向** | adapter 热加载、显存池、与 prefix 命名空间隔离 |
| **若被问到怎么挡** | 「Multi-LoRA 服务化我没在现网做 owner；同构问题是 **多租户权重切片 + 缓存命名空间**——我对 prefix/亲和隔离熟，LoRA 调度细参我不编。」→ `48` · `27` · `39` |

---

### 3 · Embedding Serving（向量/检索侧推理）

| | |
|--|--|
| **现状** | 主线是 LLM decode/PD/亲和；无 embedding 专卡 |
| **加深方向** | batch 动态、短序列吞吐、与 LLM 共享集群的队列隔离 |
| **若被问到怎么挡** | 「Embedding serving 不是我简历主交付；推理服务共性仍是 **批调度与资源隔离**——LLM 侧我有 Motor/结构化证据，embedding 具体栈我按你们组件对齐，不瞎报 QPS。」→ `39` · `11` |

---

### 4 · NIXL 深挖（协议/部署/调参）

| | |
|--|--|
| **现状** | 边界已有：[`04`](./04-SGLang-SpecOverlap与LMCache-NIXL边界.md) Part B；与 HCCL/TE 正交见 [`36`](./36-HCCL与KV传输边界卡.md) |
| **加深方向** | NixlConnector 路径、G4 运维、与 Mooncake TE 对照实验 |
| **若被问到怎么挡** | 「NIXL 我当 **数据面抽象**（Dynamo G4 / llm-d PD 常用）；我主场是 **Conductor 索引 + Mooncake 栈**，NIXL 未生产深操——不报自制 GB/s，只讲层：传≠索引。」→ `04` · `36` · `26` · `01`A |

---

### 5 · 开源贡献实物（博客 / upstream PR / RFC）

| | |
|--|--|
| **现状** | 软题提纲已承认缺口（`04` Part C · `08`）；内部交付可核，对外博客/社区 PR **待补事实** |
| **加深方向** | 脱敏博客一篇；可公开的 Motor/SO 小 PR 链；勿虚构 |
| **若被问到怎么挡** | 「对外博客和 upstream 署名贡献我还在整理；**可核**的是 MindIE 结构化输出 0→1 与 Motor 亲和相关提交——我用内部交付证明工程闭环，不编 GitHub star。」→ `04`C · `08` · `39` · `14`（LOC 待补勿编） |

---

### 6 · 附带（同级、仍不阻塞）

| 主题 | 怎么挡 | 旁链 |
|------|--------|------|
| LMCache 运维 | 平级竞品、文档级；交付链=Mooncake+Conductor | `04` B |
| Spec V2 压测曲线 | 机制+开关；**未压测不报加速比** | `04` A |
| TransportMem 细参 | 通信非主战场；集合≠点对点搬 KV | `36` |
| 手撕 LRU 题 | 算法可写 LRU；**产品缓存=FIFO/100** | `03`·`70` |
| guidance 落地 | 接口预留；生产主路径 xgrammar | `02`·`19` |

---

## 2 · 与「仍薄」的边界（勿混）

| 类型 | 代表 | 上场态度 |
|------|------|----------|
| **本清单** | 长上下文 / Multi-LoRA / Embedding / NIXL / 开源实物 | **不阻塞**；被问用挡法 |
| **唯一开放环** | `15` 实测 | 无环境→**待补**；禁编数 |
| **口径** | FIFO/100（`03`/`40`/`70`） | **已干净** |

---

## 3 · 高压模板 + 交叉引用

> 「这个点我没当 production owner / 没留对外实物，我不编。同构上讲问题形态、取舍轴、失败模式——再按你们延迟/吞吐/多租户对齐。」——场景库 [`39`](./39-高压追问应急话术卡.md)。

| 文档 | 用途 |
|------|------|
| [`04`](./04-SGLang-SpecOverlap与LMCache-NIXL边界.md) | Spec/LMCache/**NIXL**；软题/开源缺口 |
| [`36`](./36-HCCL与KV传输边界卡.md) | 传 KV / HCCL；与 NIXL 换后端同构 |
| [`39`](./39-高压追问应急话术卡.md) | 不会/没测过/没博客 挡法母本 |
| 旁链 | `67` 中场 · `41` 门禁 · `15` 实测 · `70` 口径复扫 |

---

## 4 · 验收

- [x] 主题含长上下文 / Multi-LoRA / Embedding / NIXL 深挖 / 开源实物；每项挡法  
- [x] 明确不上场阻塞；与 `15` 分开；链 `04`/`36`/`39`；**未 commit**
