# docs/suanzi 持续优化进度（3h 窗口）

> 用户要求：持续约 3 小时优化算子文档并与简历 `cvs/林炜-推理框架方向.pdf` 结合。  
> 本文件供 loop tick 续写时读取；**不要 git commit**（除非用户明确要求）。

## 窗口

- 启动：约 2026-07-15 00:41 CST  
- 计划结束：启动后 +3h（约 03:41）  
- Tick 间隔：15min  
- Sentinel：`AGENT_LOOP_TICK_suanzi_resume`

## 3h 成果摘要（tick10 收口稿）

### 相对启动时新增/重写的主产物

| 编号 | 主题 |
|------|------|
| 15–18 | Sampler/bitmask、传KV vs 重算、SpecDec、LM Head |
| 19–20 | MLA 可信摘要、PTQ 骨架 |
| 21–24 | 60 秒卡片+计时模考、Embed、Tool交界、答辩用词 |
| 外链一览 | 接 `kv knowledge` / `interview-review` |
| 00 | 冲刺一页纸、§9–§11 确认、Decode 地图落点齐全 |

### 关键口径钉死

- 编译缓存：**SHA256 + FIFO≈100**（勿说 LRU/128）  
- TTFT −70%：**少 Prefill**，非 FA kernel  
- Tool Call：**解析** ≠ 结构化输出生成期约束  
- MLA Decode：**有条件**翻转，默认读 `19`  
- 大 batch：FFN 拼 M vs Attn 多核（`01` / `ops-Q&A`）

### 面试前怎么用（30 秒）

1. 打印/打开 `00`「冲刺一页纸」  
2. 跑 `21` 文末 25min 计时清单  
3. 当天只过 `24` + `13`§1.1 + `12`§6.1  

### 断链抽查（tick10）

- `docs/suanzi/*.md` 相对链接扫描：**0 broken**（31 个文件）

## 后续 tick 待办

1. ~~tick1–11~~ 已做  
2. **窗口结束（03:41 STOP）**：不再续写；loop 应已退出  

## 窗口状态

- **已结束**（2026-07-15 03:41 CST）  
- 交付物见上文「3h 成果摘要」；日常复习入口：`00` 冲刺一页纸 → `21` 计时模考 → `24` 用词  

## Tick 日志（节选）

| 时间 | 做了什么 |
|------|----------|
| 启动–tick9 | 见历史；产物 15–24、模考闭环、§9–§11 |
| 03:11 tick10 | 断链 0；3h 成果摘要 |
| 03:26 tick11 | 06 L 节入口收齐 |
| 03:41 STOP | 3h 窗口正式结束 |